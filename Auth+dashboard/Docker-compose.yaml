services:
  auth:
    build:
      context: ./auth
    container_name: auth
    ports:
      - "5000:5000"

  dashboard:
    build:
      context: ./dashboard
    container_name: dashboard
    ports:
      - "5001:5001"
    depends_on:
      - auth
