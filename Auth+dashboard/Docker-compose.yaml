services:
  auth:
    build:
      context: ./auth
    container_name: auth
    ports:
      - "5000:5000"
    environment:
      - DATABASE_URL=mysql+mysqldb://root:mysqlpassword123@db/orchestratorAuth
    depends_on:
      - db

  dashboard:
    build:
      context: ./dashboard
    container_name: dashboard
    ports:
      - "5001:5001"
    depends_on:
      - auth

  db:
    image: mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=mysqlpassword123
      - MYSQL_DATABASE=orchestratorAuth
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: